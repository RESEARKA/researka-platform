import React from 'react';
import ClientOnlyProfile from './profile';

/**
 * This is a transitional component that uses the new modular architecture
 * while maintaining backward compatibility with existing code.
 * It simply re-exports the modular ClientOnlyProfile component.
 */
export default ClientOnlyProfile;
